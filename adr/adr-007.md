<!-- markdownlint-disable MD041 MD033 -->
<img src="../images/rawry-150.png" alt="Rawry" style="float: right; margin: 10px;">

# ADR-007: Serializable `data`

[ADRs](./README.md)

## Status

💡 **Proposed**

> An idea to be considered for adoption

## Context

Data structures are often used as DTOs or for persistence.

## Decision

Every `data` type should be serializable by default. A `trait` for custom serialization can be added. This trait can be used by `object` types as well, though they will not be serializable without it.

Replicate the `Codable` protocol from Swift. I.e. don't assume JSON output, but allow encoding in any format (including YAML, XML and even non-textual binary/ternary).

## Rationale

1. Persistence and communication (DTO/DAO) is the main purpose [^big-data] for a data structure. And serialisation is a fundamental strategy for that use case.
2. If encapsulated objects are persisted, it is assumed that the developer will want to define the strategy themself.

[^big-data]: Data structures actually exist to support **two** main use cases: the other case is easy readonly access for computations on — and filtering of — big data.

## Alternatives Considered

- Requiring explicit annotation and implementation for all serialized types.
- Requiring explicit annotation, but providing a default implementation based on declared fields (like Swift).

## Consequences

### Positive

- Simple and automatic serialization

### Negative

- The serialization protocol must be designed

## Related ADRs

- [ADR-002](./adr-002.md) (Encapsulation vs Data)
- [ADR-008](./adr-008.md) (Protocol Types)
